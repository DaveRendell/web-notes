// A missing vault image should exercise the real NoteImage placeholder without
// loading the browser app's Drive/auth/cache providers into this prototype.
export function useImages() { return null; }
